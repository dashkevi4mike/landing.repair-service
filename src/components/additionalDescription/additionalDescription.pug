mixin additionalDescription(options)
  - var elClass=`additional-description__text_theme_${options.theme}`

  .additional-description
    if options.withLine
      .additional-description__short-line
    p.additional-description__text(class=elClass)=options.text